
## 14:48 - 会员体系4项修复

用户提出4个修复需求：

### 修复内容

**1. 去掉本月配额使用模块**
- `views/membership.php` — 删除整个 `.quota-section` 块（配额条HTML + CSS），保留会员方案卡

**2. 等待审核页面：去掉微信pigeon_cs，修正联系客服链接**
- `views/upgrade.php` — 删除"微信：**pigeon_cs**"行，`/contact` 改为 `/pages/contact/`

**3. 查看订单时，会员待审核订单跳转到等待审核页**
- `app/models/Payment.php` — `getUserOrders()` 的 `plan_name` CASE 改为显示"月度VIP"/"年度VIP"（基于plan_type）
- `views/my_orders.php` — 会员订单 status=0 时显示"待审核"、橙色"查看详情"按钮跳转到 `/upgrade?plan_type=X&status=pending&order_no=XXX`；非会员订单保持"待支付"+"去支付"

**4. 后台订单管理产品列改为月度/年度会员**
- `views/admin/orders.php` — 产品列旧版「铜牌→金牌→金牌」改为「月付」/「年付」标签 + 金额/周期显示；订单详情弹窗同步
- `app/controllers/AdminController.php` — `updateOrder()` 增加兼容逻辑：`months=0` 时按 `plan_type` 推算（1→1月，2→12月），`to_level=0` 时默认 VIP(1)

### 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `views/membership.php` | 删配额模块（HTML+CSS），保留会员方案卡片 |
| `views/upgrade.php` | 删pigeon_cs，fix `/pages/contact/` |
| `app/models/Payment.php` | `getUserOrders()` plan_name CASE 改为月度/年度VIP |
| `views/my_orders.php` | 会员待审核显示+链接 |
| `views/admin/orders.php` | 产品列改为月付/年付标签 |
| `app/controllers/AdminController.php` | updateOrder() 兼容新旧订单 |

### 待上传
所有修改均在本地完成，尚未上传至服务器。
